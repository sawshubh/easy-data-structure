#include<stdio.h>
char findNumber(int arr[], int k)
{
   int f,l;
   while(f>=l)
   {
      
   }
}
int main()
{
   int arr;
   int k;

   for (int i = 0; i < 5; i++)
   {
      scanf("%d",&arr); //five elements to be entered
   }
   scanf("%d",&k); //k element to be search

   findNumber(k,arr);
   return 0;
}